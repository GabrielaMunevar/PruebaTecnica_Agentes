# Proveedor y modelo
LLM_PROVIDER=openai
LLM_MODEL=gpt-4.1-mini

# Configuración de generación
LLM_TEMPERATURE=0
LLM_TIMEOUT_SECONDS=60
LLM_MAX_RETRIES=2

# La llave real debe configurarse únicamente en .env.
# El nombre depende del proveedor seleccionado.
OPENAI_API_KEY=